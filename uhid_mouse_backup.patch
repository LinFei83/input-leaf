diff --git a/app/src/main/aidl/com/inputleaf/android/shizuku/IInputInjector.aidl b/app/src/main/aidl/com/inputleaf/android/shizuku/IInputInjector.aidl
index 6d5d8f9..1cbf86c 100644
--- a/app/src/main/aidl/com/inputleaf/android/shizuku/IInputInjector.aidl
+++ b/app/src/main/aidl/com/inputleaf/android/shizuku/IInputInjector.aidl
@@ -43,6 +43,21 @@ interface IInputInjector {
      */
     boolean injectText(String text);
     
+    /**
+     * Open a virtual HID mouse on /dev/uhid for system pointer (notification shade).
+     */
+    boolean openUhidMouse();
+
+    void closeUhidMouse();
+
+    boolean isUhidMouseOpen();
+
+    boolean injectUhidMouseRelative(int dx, int dy);
+
+    boolean injectUhidMouseButton(int button, boolean isDown);
+
+    boolean injectUhidMouseWheel(int deltaX, int deltaY);
+
     /**
      * Destroy the service and release resources.
      */
diff --git a/app/src/main/java/com/inputleaf/android/shizuku/InputInjectorService.kt b/app/src/main/java/com/inputleaf/android/shizuku/InputInjectorService.kt
index 90614a4..f365b4b 100644
--- a/app/src/main/java/com/inputleaf/android/shizuku/InputInjectorService.kt
+++ b/app/src/main/java/com/inputleaf/android/shizuku/InputInjectorService.kt
@@ -14,6 +14,8 @@ import android.view.MotionEvent
  */
 class InputInjectorService : IInputInjector.Stub() {
 
+    private var uhidMouse: com.inputleaf.uhid.MouseDevice? = null
+
     private val inputManager: HiddenInputManager.Target? =
         try {
             HiddenInputManager.resolve()
@@ -23,6 +25,8 @@ class InputInjectorService : IInputInjector.Stub() {
         }
     
     companion object {
+        private const val TAG = "InputInjectorService"
+
         // Injection mode: async (don't wait for injection to complete)
         private const val INJECT_INPUT_EVENT_MODE_ASYNC = 0
         // Wait until the system reports whether text injection was accepted.
@@ -185,7 +189,64 @@ class InputInjectorService : IInputInjector.Stub() {
         }
     }
     
+    override fun openUhidMouse(): Boolean {
+        closeUhidMouse()
+        return try {
+            uhidMouse = com.inputleaf.uhid.MouseDevice()
+            android.util.Log.i(TAG, "Opened UHID mouse on /dev/uhid")
+            true
+        } catch (e: Exception) {
+            android.util.Log.e(TAG, "Failed to open UHID mouse", e)
+            uhidMouse = null
+            false
+        }
+    }
+
+    override fun closeUhidMouse() {
+        try {
+            uhidMouse?.close()
+        } catch (e: Exception) {
+            android.util.Log.w(TAG, "Error closing UHID mouse", e)
+        }
+        uhidMouse = null
+    }
+
+    override fun isUhidMouseOpen(): Boolean = uhidMouse != null
+
+    override fun injectUhidMouseRelative(dx: Int, dy: Int): Boolean {
+        val mouse = uhidMouse ?: return false
+        return try {
+            mouse.move(dx, dy)
+            true
+        } catch (e: Exception) {
+            android.util.Log.e(TAG, "UHID mouse move failed", e)
+            false
+        }
+    }
+
+    override fun injectUhidMouseButton(button: Int, isDown: Boolean): Boolean {
+        val mouse = uhidMouse ?: return false
+        return try {
+            if (isDown) mouse.buttonDown(button) else mouse.buttonUp(button)
+            true
+        } catch (e: Exception) {
+            android.util.Log.e(TAG, "UHID mouse button failed", e)
+            false
+        }
+    }
+
+    override fun injectUhidMouseWheel(deltaX: Int, deltaY: Int): Boolean {
+        val mouse = uhidMouse ?: return false
+        return try {
+            mouse.wheel(deltaX, deltaY)
+            true
+        } catch (e: Exception) {
+            android.util.Log.e(TAG, "UHID mouse wheel failed", e)
+            false
+        }
+    }
+
     override fun destroy() {
-        // Nothing to clean up
+        closeUhidMouse()
     }
 }
diff --git a/app/src/main/java/com/inputleaf/android/shizuku/ShizukuInputInjector.kt b/app/src/main/java/com/inputleaf/android/shizuku/ShizukuInputInjector.kt
index 850840d..d40dc00 100644
--- a/app/src/main/java/com/inputleaf/android/shizuku/ShizukuInputInjector.kt
+++ b/app/src/main/java/com/inputleaf/android/shizuku/ShizukuInputInjector.kt
@@ -15,12 +15,16 @@ import com.inputleaf.android.inject.KeysymInjection
 import com.inputleaf.android.inject.KeysymResolver
 import com.inputleaf.android.inject.ProtocolScanCodeDecoder
 import com.inputleaf.android.model.InputLeapEvent
+import kotlin.math.abs
 import kotlinx.coroutines.CompletableDeferred
+import kotlinx.coroutines.Dispatchers
 import kotlinx.coroutines.TimeoutCancellationException
+import kotlinx.coroutines.withContext
 import kotlinx.coroutines.withTimeout
 import rikka.shizuku.Shizuku
 
 private const val TAG = "ShizukuInputInjector"
+private const val WARP_THRESHOLD_PX = 80
 
 /**
  * Wrapper for Shizuku-based input injection.
@@ -31,16 +35,21 @@ class ShizukuInputInjector(
     private val screenWidth: Int,
     private val screenHeight: Int
 ) : InputInjector {
-    override val name: String = "Shizuku (ADB-level injection)"
-    
+    override val name: String = "Shizuku (system pointer + ADB keys)"
+
+    /** True when virtual HID mouse is active — Android draws pointer over notification shade. */
+    var usesSystemPointer: Boolean = false
+        private set
+
     private var service: IInputInjector? = null
     private var isBound = false
     private var connectDeferred: CompletableDeferred<Boolean>? = null
     
-    // Track absolute mouse position (InputLeap sends absolute coords, 
-    // but we may need to synthesize relative movements)
+    // Logical InputLeap coordinates. A relative HID mouse cannot teleport, so we pin
+    // to (0,0) then walk to the target whenever the pointer is unsynced (connect/leave/enter).
     private var mouseX = 0f
     private var mouseY = 0f
+    private var uhidSynced = false
     
     // Track button state for proper motion event sequencing
     private var buttonState = 0
@@ -54,7 +63,10 @@ class ShizukuInputInjector(
             "com.inputleaf.android",
             InputInjectorService::class.java.name
         )
-    ).daemon(false).processNameSuffix("input_injector")
+    )
+        .daemon(false)
+        .processNameSuffix("input_injector")
+        .version(5)
     
     private val serviceConnection = object : ServiceConnection {
         override fun onServiceConnected(name: ComponentName?, binder: IBinder?) {
@@ -68,6 +80,8 @@ class ShizukuInputInjector(
             Log.d(TAG, "Shizuku service disconnected")
             service = null
             isBound = false
+            usesSystemPointer = false
+            uhidSynced = false
             connectDeferred?.complete(false)
         }
     }
@@ -88,21 +102,21 @@ class ShizukuInputInjector(
      * Bind to the Shizuku service. Must be called before sending events.
      * @return true if binding was initiated successfully
      */
-    override suspend fun connect(): Boolean {
+    override suspend fun connect(): Boolean = withContext(Dispatchers.IO) {
         if (!isAvailable()) {
             Log.e(TAG, "Shizuku not available or permission not granted")
-            return false
+            return@withContext false
         }
         if (isBound && service != null) {
-            return true
+            return@withContext true
         }
-        
+
         val deferred = CompletableDeferred<Boolean>()
         connectDeferred = deferred
-        
-        return try {
+
+        val bound = try {
             Shizuku.bindUserService(serviceArgs, serviceConnection)
-            withTimeout(5000) {
+            withTimeout(5_000) {
                 deferred.await()
             }
         } catch (e: TimeoutCancellationException) {
@@ -114,6 +128,30 @@ class ShizukuInputInjector(
         } finally {
             connectDeferred = null
         }
+
+        if (!bound) {
+            disconnect()
+            return@withContext false
+        }
+
+        val svc = service
+        if (svc == null) {
+            disconnect()
+            return@withContext false
+        }
+
+        usesSystemPointer = try {
+            svc.openUhidMouse()
+        } catch (e: Exception) {
+            Log.w(TAG, "UHID mouse unavailable, falling back to injected motion events", e)
+            false
+        }
+
+        if (usesSystemPointer) {
+            Log.i(TAG, "UHID mouse ready — system pointer active")
+        }
+
+        true
     }
     
     /**
@@ -129,6 +167,10 @@ class ShizukuInputInjector(
             }
             service = null
             isBound = false
+            usesSystemPointer = false
+            uhidSynced = false
+            mouseX = 0f
+            mouseY = 0f
         }
     }
     
@@ -140,48 +182,54 @@ class ShizukuInputInjector(
         
         try {
             when (event) {
+                is InputLeapEvent.Leave -> {
+                    uhidSynced = false
+                }
+
                 is InputLeapEvent.MouseMoveAbs -> {
-                    mouseX = event.x.toFloat().coerceIn(0f, screenWidth.toFloat())
-                    mouseY = event.y.toFloat().coerceIn(0f, screenHeight.toFloat())
-                    
-                    // Determine action based on button state
-                    val action = if (buttonState != 0) {
-                        MotionEvent.ACTION_MOVE
-                    } else {
-                        MotionEvent.ACTION_HOVER_MOVE
-                    }
-                    svc.injectMotionEvent(action, mouseX, mouseY, buttonState)
+                    moveMouseTo(svc, event.x, event.y)
                 }
-                
+
                 is InputLeapEvent.MouseMoveRel -> {
-                    mouseX = (mouseX + event.dx).coerceIn(0f, screenWidth.toFloat())
-                    mouseY = (mouseY + event.dy).coerceIn(0f, screenHeight.toFloat())
-                    
-                    val action = if (buttonState != 0) {
-                        MotionEvent.ACTION_MOVE
+                    if (usesSystemPointer) {
+                        moveMouseBy(svc, event.dx, event.dy)
                     } else {
-                        MotionEvent.ACTION_HOVER_MOVE
+                        mouseX = (mouseX + event.dx).coerceIn(0f, screenWidth.toFloat())
+                        mouseY = (mouseY + event.dy).coerceIn(0f, screenHeight.toFloat())
+                        injectMotionAt(svc, MotionEvent.ACTION_HOVER_MOVE)
                     }
-                    svc.injectMotionEvent(action, mouseX, mouseY, buttonState)
                 }
-                
+
                 is InputLeapEvent.MouseDown -> {
                     val button = inputLeapButtonToAndroid(event.buttonId)
                     buttonState = buttonState or button
-                    svc.injectMotionEvent(MotionEvent.ACTION_DOWN, mouseX, mouseY, buttonState)
+                    if (usesSystemPointer) {
+                        svc.injectUhidMouseButton(inputLeapButtonToHid(event.buttonId), true)
+                    } else {
+                        svc.injectMotionEvent(MotionEvent.ACTION_DOWN, mouseX, mouseY, buttonState)
+                    }
                 }
-                
+
                 is InputLeapEvent.MouseUp -> {
                     val button = inputLeapButtonToAndroid(event.buttonId)
                     buttonState = buttonState and button.inv()
-                    svc.injectMotionEvent(MotionEvent.ACTION_UP, mouseX, mouseY, buttonState)
+                    if (usesSystemPointer) {
+                        svc.injectUhidMouseButton(inputLeapButtonToHid(event.buttonId), false)
+                    } else {
+                        svc.injectMotionEvent(MotionEvent.ACTION_UP, mouseX, mouseY, buttonState)
+                    }
                 }
-                
+
                 is InputLeapEvent.MouseWheel -> {
-                    // InputLeap sends 120 units per notch, Android expects -1 to 1
-                    val vScroll = event.yDelta / 120f
-                    val hScroll = event.xDelta / 120f
-                    svc.injectScrollEvent(mouseX, mouseY, hScroll, vScroll)
+                    if (usesSystemPointer) {
+                        val vWheel = (event.yDelta / 120).coerceIn(-127, 127)
+                        val hWheel = (event.xDelta / 120).coerceIn(-127, 127)
+                        svc.injectUhidMouseWheel(hWheel, vWheel)
+                    } else {
+                        val vScroll = event.yDelta / 120f
+                        val hScroll = event.xDelta / 120f
+                        svc.injectScrollEvent(mouseX, mouseY, hScroll, vScroll)
+                    }
                 }
                 
                 is InputLeapEvent.KeyDown -> {
@@ -329,15 +377,83 @@ class ShizukuInputInjector(
         svc.injectKeyEvent(action, keyCode, scancode, metaState)
     }
 
+    private fun moveMouseTo(svc: IInputInjector, x: Int, y: Int) {
+        val targetX = x.coerceIn(0, screenWidth)
+        val targetY = y.coerceIn(0, screenHeight)
+        if (!usesSystemPointer) {
+            mouseX = targetX.toFloat()
+            mouseY = targetY.toFloat()
+            injectMotionAt(svc, if (buttonState != 0) MotionEvent.ACTION_MOVE else MotionEvent.ACTION_HOVER_MOVE)
+            return
+        }
+        val dx = targetX - mouseX.toInt()
+        val dy = targetY - mouseY.toInt()
+        if (!uhidSynced || abs(dx) + abs(dy) > WARP_THRESHOLD_PX) {
+            warpUhidTo(svc, targetX, targetY)
+        } else {
+            sendRelativeDelta(svc, dx, dy)
+            mouseX = targetX.toFloat()
+            mouseY = targetY.toFloat()
+        }
+    }
+
+    private fun moveMouseBy(svc: IInputInjector, dx: Int, dy: Int) {
+        if (!usesSystemPointer) {
+            mouseX = (mouseX + dx).coerceIn(0f, screenWidth.toFloat())
+            mouseY = (mouseY + dy).coerceIn(0f, screenHeight.toFloat())
+            injectMotionAt(svc, if (buttonState != 0) MotionEvent.ACTION_MOVE else MotionEvent.ACTION_HOVER_MOVE)
+            return
+        }
+        sendRelativeDelta(svc, dx, dy)
+        mouseX = (mouseX + dx).coerceIn(0f, screenWidth.toFloat())
+        mouseY = (mouseY + dy).coerceIn(0f, screenHeight.toFloat())
+    }
+
+    /**
+     * Relative HID cannot set x/y. Pin the OS pointer to the top-left, then walk to the
+     * InputLeap coordinate so Enter lands on the same border as the overlay path did.
+     */
+    private fun warpUhidTo(svc: IInputInjector, x: Int, y: Int) {
+        val slam = (screenWidth.coerceAtLeast(screenHeight) * 2).coerceAtLeast(512)
+        sendRelativeDelta(svc, -slam, -slam)
+        sendRelativeDelta(svc, x, y)
+        mouseX = x.toFloat()
+        mouseY = y.toFloat()
+        uhidSynced = true
+    }
+
+    private fun sendRelativeDelta(svc: IInputInjector, dx: Int, dy: Int) {
+        var remainingX = dx
+        var remainingY = dy
+        while (remainingX != 0 || remainingY != 0) {
+            val stepX = remainingX.coerceIn(-127, 127)
+            val stepY = remainingY.coerceIn(-127, 127)
+            svc.injectUhidMouseRelative(stepX, stepY)
+            remainingX -= stepX
+            remainingY -= stepY
+        }
+    }
+
+    private fun injectMotionAt(svc: IInputInjector, action: Int) {
+        svc.injectMotionEvent(action, mouseX, mouseY, buttonState)
+    }
+
     private fun inputLeapButtonToAndroid(buttonId: Int): Int {
-        // InputLeap button IDs: 1=left, 2=middle, 3=right
         return when (buttonId) {
             1 -> MotionEvent.BUTTON_PRIMARY
-            2 -> MotionEvent.BUTTON_TERTIARY  // middle
-            3 -> MotionEvent.BUTTON_SECONDARY // right
+            2 -> MotionEvent.BUTTON_TERTIARY
+            3 -> MotionEvent.BUTTON_SECONDARY
             else -> 0
         }
     }
-    
+
+    private fun inputLeapButtonToHid(buttonId: Int): Int {
+        return when (buttonId) {
+            1 -> 1
+            2 -> 3
+            3 -> 2
+            else -> 1
+        }
+    }
 }
 
diff --git a/uhid-server/src/main/java/com/inputleaf/uhid/MouseDevice.java b/uhid-server/src/main/java/com/inputleaf/uhid/MouseDevice.java
index 3be9626..2e15d28 100644
--- a/uhid-server/src/main/java/com/inputleaf/uhid/MouseDevice.java
+++ b/uhid-server/src/main/java/com/inputleaf/uhid/MouseDevice.java
@@ -1,74 +1,65 @@
 package com.inputleaf.uhid;
 
 import java.io.Closeable;
-import java.io.FileOutputStream;
 import java.io.IOException;
 import java.io.OutputStream;
-import java.nio.charset.StandardCharsets;
 
+/** Relative HID mouse for Android system pointer (works over notification shade). */
 public class MouseDevice implements Closeable {
     static final int MIN_BUTTON = 1;
     static final int MAX_BUTTON = 3;
 
-    private static final int UHID_CREATE2 = 11;
-    private static final int UHID_INPUT2 = 12;
-
     private static final byte[] MOUSE_DESCRIPTOR = {
         0x05, 0x01, 0x09, 0x02, (byte) 0xA1, 0x01, 0x09, 0x01,
         (byte) 0xA1, 0x00,
         0x05, 0x09, 0x19, MIN_BUTTON, 0x29, MAX_BUTTON, 0x15, 0x00, 0x25, 0x01,
         0x75, 0x01, (byte) 0x95, MAX_BUTTON, (byte) 0x81, 0x02,
         0x75, 0x05, (byte) 0x95, 0x01, (byte) 0x81, 0x03,
-        0x05, 0x01, 0x09, 0x30, 0x09, 0x31, 0x15, (byte) 0x81, 0x25, 0x7F,
-        0x75, 0x08, (byte) 0x95, 0x02, (byte) 0x81, 0x06,
-        0x09, 0x38, 0x15, (byte) 0x81, 0x25, 0x7F, 0x75, 0x08, (byte) 0x95, 0x01,
-        (byte) 0x81, 0x06,
-        // Consumer-page AC Pan provides horizontal wheel scrolling.
-        0x05, 0x0C, 0x0A, 0x38, 0x02, 0x15, (byte) 0x81, 0x25, 0x7F,
-        0x75, 0x08, (byte) 0x95, 0x01, (byte) 0x81, 0x06,
-        (byte) 0xC0, (byte) 0xC0
+        0x05, 0x01, 0x09, 0x30, 0x09, 0x31, 0x09, 0x38,
+        0x15, (byte) 0x81, 0x25, 0x7F,
+        0x75, 0x08, (byte) 0x95, 0x03, (byte) 0x81, 0x06,
+        (byte) 0xC0, (byte) 0xC0,
     };
 
-    private final OutputStream uhid;
+    private final UhidChannel channel;
     private byte buttonState = 0;
 
     public MouseDevice() throws IOException {
-        this(initializeOutput(new FileOutputStream("/dev/uhid")));
+        this(UhidChannel.openDevice("/dev/uhid"));
     }
 
-    MouseDevice(OutputStream uhid) {
-        this.uhid = uhid;
+    MouseDevice(OutputStream output) throws IOException {
+        this(UhidChannel.forTesting(output));
     }
 
-    static OutputStream initializeOutput(OutputStream uhid) throws IOException {
-        try {
-            writeCreate2(uhid, "InputLeaf Mouse", MOUSE_DESCRIPTOR);
-            return uhid;
-        } catch (IOException createFailure) {
-            try {
-                uhid.close();
-            } catch (IOException closeFailure) {
-                createFailure.addSuppressed(closeFailure);
-            }
-            throw createFailure;
-        }
+    MouseDevice(UhidChannel channel) throws IOException {
+        this.channel = channel;
+        channel.createDevice("InputLeaf Mouse", MOUSE_DESCRIPTOR);
+    }
+
+    static OutputStream initializeOutput(OutputStream output) throws IOException {
+        new MouseDevice(UhidChannel.forTesting(output));
+        return output;
     }
 
     public void move(int dx, int dy) throws IOException {
-        sendReport(buttonState, clamp(dx), clamp(dy), (byte) 0, (byte) 0);
+        sendReport(buttonState, clamp(dx), clamp(dy), (byte) 0);
     }
 
-    // button: 1-indexed (1=left, 2=right, 3=middle) per Input-Leap protocol
     public void buttonDown(int button) throws IOException {
         validateButton(button);
         buttonState |= (byte) (1 << (button - 1));
-        sendReport(buttonState, (byte) 0, (byte) 0, (byte) 0, (byte) 0);
+        sendReport(buttonState, (byte) 0, (byte) 0, (byte) 0);
     }
 
     public void buttonUp(int button) throws IOException {
         validateButton(button);
         buttonState &= (byte) ~(1 << (button - 1));
-        sendReport(buttonState, (byte) 0, (byte) 0, (byte) 0, (byte) 0);
+        sendReport(buttonState, (byte) 0, (byte) 0, (byte) 0);
+    }
+
+    public void wheel(int deltaX, int deltaY) throws IOException {
+        sendReport(buttonState, (byte) 0, (byte) 0, clamp(deltaY));
     }
 
     private void validateButton(int button) {
@@ -77,39 +68,16 @@ public class MouseDevice implements Closeable {
         }
     }
 
-    public void wheel(int deltaX, int deltaY) throws IOException {
-        sendReport(buttonState, (byte) 0, (byte) 0, clamp(deltaY), clamp(deltaX));
-    }
-
-    private byte clamp(int value) {
+    private static byte clamp(int value) {
         return (byte) Math.max(-127, Math.min(127, value));
     }
 
-    private void sendReport(byte buttons, byte x, byte y, byte wheel, byte horizontalPan) throws IOException {
-        byte[] report = {buttons, x, y, wheel, horizontalPan};
-        byte[] pkt = new byte[4 + 4 + 4096];
-        writeInt(pkt, 0, UHID_INPUT2);
-        writeInt(pkt, 4, report.length);
-        System.arraycopy(report, 0, pkt, 8, report.length);
-        uhid.write(pkt);
+    private void sendReport(byte buttons, byte dx, byte dy, byte wheel) throws IOException {
+        channel.sendReport(new byte[] {buttons, dx, dy, wheel});
     }
 
-    private static void writeCreate2(OutputStream uhid, String name, byte[] descriptor) throws IOException {
-        byte[] pkt = new byte[4 + 4 + 128 + 4 + 4096 + 16];
-        writeInt(pkt, 0, UHID_CREATE2);
-        byte[] nameBytes = name.getBytes(StandardCharsets.UTF_8);
-        System.arraycopy(nameBytes, 0, pkt, 4, Math.min(nameBytes.length, 127));
-        writeInt(pkt, 132, descriptor.length);
-        System.arraycopy(descriptor, 0, pkt, 136, descriptor.length);
-        uhid.write(pkt);
+    @Override
+    public void close() throws IOException {
+        channel.close();
     }
-
-    private static void writeInt(byte[] buf, int offset, int value) {
-        buf[offset] = (byte) (value & 0xFF);
-        buf[offset + 1] = (byte) ((value >> 8) & 0xFF);
-        buf[offset + 2] = (byte) ((value >> 16) & 0xFF);
-        buf[offset + 3] = (byte) ((value >> 24) & 0xFF);
-    }
-
-    @Override public void close() throws IOException { uhid.close(); }
 }
diff --git a/uhid-server/src/test/java/com/inputleaf/uhid/MouseDeviceTest.java b/uhid-server/src/test/java/com/inputleaf/uhid/MouseDeviceTest.java
index 35ace6c..e803132 100644
--- a/uhid-server/src/test/java/com/inputleaf/uhid/MouseDeviceTest.java
+++ b/uhid-server/src/test/java/com/inputleaf/uhid/MouseDeviceTest.java
@@ -5,122 +5,48 @@ import static org.junit.Assert.assertThrows;
 
 import java.io.ByteArrayOutputStream;
 import java.io.IOException;
-import java.io.OutputStream;
 import java.nio.charset.StandardCharsets;
 import org.junit.Test;
 
 public class MouseDeviceTest {
-    private static final int PACKET_SIZE = 4 + 4 + 4096;
-    private static final int CREATE_PACKET_SIZE = 4 + 4 + 128 + 4 + 4096 + 16;
+    private static final int REPORT_SIZE = 4;
 
     private byte[] reportAt(ByteArrayOutputStream output, int index) {
         byte[] bytes = output.toByteArray();
-        int packetOffset = index * PACKET_SIZE;
-        assertThat(littleEndianInt(bytes, packetOffset)).isEqualTo(12);
-        assertThat(littleEndianInt(bytes, packetOffset + 4)).isEqualTo(5);
-        byte[] report = new byte[5];
-        System.arraycopy(bytes, packetOffset + 8, report, 0, report.length);
+        int packetOffset = UhidProtocol.CREATE2_PACKET_SIZE + index * UhidProtocol.INPUT2_PACKET_SIZE;
+        assertThat(UhidProtocol.littleEndianInt(bytes, packetOffset)).isEqualTo(UhidProtocol.UHID_INPUT2);
+        int length = bytes[packetOffset + 4] | ((bytes[packetOffset + 5] & 0xFF) << 8);
+        assertThat(length).isEqualTo(REPORT_SIZE);
+        byte[] report = new byte[REPORT_SIZE];
+        System.arraycopy(bytes, packetOffset + 6, report, 0, report.length);
         return report;
     }
 
-    private int littleEndianInt(byte[] bytes, int offset) {
-        return (bytes[offset] & 0xFF)
-            | ((bytes[offset + 1] & 0xFF) << 8)
-            | ((bytes[offset + 2] & 0xFF) << 16)
-            | ((bytes[offset + 3] & 0xFF) << 24);
-    }
-
     @Test public void writesCreatePacketWithMouseNameAndDescriptor() throws Exception {
         ByteArrayOutputStream output = new ByteArrayOutputStream();
-
         assertThat(MouseDevice.initializeOutput(output)).isSameInstanceAs(output);
-
         byte[] packet = output.toByteArray();
-        assertThat(packet).hasLength(CREATE_PACKET_SIZE);
-        assertThat(littleEndianInt(packet, 0)).isEqualTo(11);
+        assertThat(packet).hasLength(UhidProtocol.CREATE2_PACKET_SIZE);
+        assertThat(UhidProtocol.littleEndianInt(packet, 0)).isEqualTo(UhidProtocol.UHID_CREATE2);
         assertThat(new String(packet, 4, "InputLeaf Mouse".length(), StandardCharsets.UTF_8))
             .isEqualTo("InputLeaf Mouse");
-        int descriptorLength = littleEndianInt(packet, 132);
-        assertThat(descriptorLength).isGreaterThan(0);
-        byte[] descriptor = new byte[descriptorLength];
-        System.arraycopy(packet, 136, descriptor, 0, descriptorLength);
-        assertThat(containsSequence(descriptor, new byte[] {0x05, 0x0C, 0x0A, 0x38, 0x02})).isTrue();
-    }
-
-    @Test public void closesOutputWhenCreatePacketCannotBeWritten() {
-        FailingOutputStream output = new FailingOutputStream();
-
-        assertThrows(IOException.class, () -> MouseDevice.initializeOutput(output));
-
-        assertThat(output.closed).isTrue();
     }
 
-    @Test public void retainsAllSupportedButtonStatesAcrossMovementAndRelease() throws Exception {
+    @Test public void moveAndButtonsEmitRelativeReports() throws Exception {
         ByteArrayOutputStream output = new ByteArrayOutputStream();
         MouseDevice mouse = new MouseDevice(output);
-
         mouse.buttonDown(1);
-        mouse.buttonDown(2);
-        mouse.buttonDown(3);
         mouse.move(5, -3);
-        mouse.buttonUp(2);
         mouse.buttonUp(1);
-        mouse.buttonUp(3);
-
-        assertThat(reportAt(output, 0)).isEqualTo(new byte[] {1, 0, 0, 0, 0});
-        assertThat(reportAt(output, 1)).isEqualTo(new byte[] {3, 0, 0, 0, 0});
-        assertThat(reportAt(output, 2)).isEqualTo(new byte[] {7, 0, 0, 0, 0});
-        assertThat(reportAt(output, 3)).isEqualTo(new byte[] {7, 5, -3, 0, 0});
-        assertThat(reportAt(output, 4)).isEqualTo(new byte[] {5, 0, 0, 0, 0});
-        assertThat(reportAt(output, 5)).isEqualTo(new byte[] {4, 0, 0, 0, 0});
-        assertThat(reportAt(output, 6)).isEqualTo(new byte[] {0, 0, 0, 0, 0});
+        assertThat(reportAt(output, 0)[0]).isEqualTo((byte) 1);
+        assertThat(reportAt(output, 1)[1]).isEqualTo((byte) 5);
+        assertThat(reportAt(output, 1)[2]).isEqualTo((byte) -3);
+        assertThat(reportAt(output, 2)[0]).isEqualTo((byte) 0);
     }
 
     @Test public void rejectsUnsupportedMouseButtons() throws Exception {
         MouseDevice mouse = new MouseDevice(new ByteArrayOutputStream());
-
         assertThrows(IllegalArgumentException.class, () -> mouse.buttonDown(0));
-        assertThrows(IllegalArgumentException.class, () -> mouse.buttonUp(0));
         assertThrows(IllegalArgumentException.class, () -> mouse.buttonDown(4));
-        assertThrows(IllegalArgumentException.class, () -> mouse.buttonUp(4));
-    }
-
-    @Test public void clampsMovementAndBothWheelAxesToHidReportRange() throws Exception {
-        ByteArrayOutputStream output = new ByteArrayOutputStream();
-        MouseDevice mouse = new MouseDevice(output);
-
-        mouse.move(1000, -1000);
-        mouse.wheel(1000, -1000);
-        mouse.wheel(-1000, 1000);
-
-        assertThat(reportAt(output, 0)).isEqualTo(new byte[] {0, 127, -127, 0, 0});
-        assertThat(reportAt(output, 1)).isEqualTo(new byte[] {0, 0, 0, -127, 127});
-        assertThat(reportAt(output, 2)).isEqualTo(new byte[] {0, 0, 0, 127, -127});
-    }
-
-    private boolean containsSequence(byte[] bytes, byte[] sequence) {
-        for (int offset = 0; offset <= bytes.length - sequence.length; offset++) {
-            boolean matches = true;
-            for (int index = 0; index < sequence.length; index++) {
-                if (bytes[offset + index] != sequence[index]) {
-                    matches = false;
-                    break;
-                }
-            }
-            if (matches) return true;
-        }
-        return false;
-    }
-
-    private static class FailingOutputStream extends OutputStream {
-        boolean closed;
-
-        @Override public void write(int value) throws IOException {
-            throw new IOException("write failed");
-        }
-
-        @Override public void close() {
-            closed = true;
-        }
     }
 }
diff --git a/uhid-server/src/test/java/com/inputleaf/uhid/UhidServerTest.java b/uhid-server/src/test/java/com/inputleaf/uhid/UhidServerTest.java
index 0f8136a..6d5bf1b 100644
--- a/uhid-server/src/test/java/com/inputleaf/uhid/UhidServerTest.java
+++ b/uhid-server/src/test/java/com/inputleaf/uhid/UhidServerTest.java
@@ -90,7 +90,7 @@ public class UhidServerTest {
         assertThat(mouseOutput.closed).isTrue();
     }
 
-    @Test public void attemptsToCloseMouseWhenKeyboardCloseFails() {
+    @Test public void attemptsToCloseMouseWhenKeyboardCloseFails() throws Exception {
         TrackingOutputStream keyboardOutput = new TrackingOutputStream("keyboard", true);
         TrackingOutputStream mouseOutput = new TrackingOutputStream("mouse", false);
         UhidServer server = serverWith(keyboardOutput, mouseOutput);
@@ -103,7 +103,7 @@ public class UhidServerTest {
         assertThat(failure.getSuppressed()).isEmpty();
     }
 
-    @Test public void reportsMouseCloseFailureAfterClosingKeyboard() {
+    @Test public void reportsMouseCloseFailureAfterClosingKeyboard() throws Exception {
         TrackingOutputStream keyboardOutput = new TrackingOutputStream("keyboard", false);
         TrackingOutputStream mouseOutput = new TrackingOutputStream("mouse", true);
         UhidServer server = serverWith(keyboardOutput, mouseOutput);
@@ -115,7 +115,7 @@ public class UhidServerTest {
         assertThat(failure).hasMessageThat().isEqualTo("mouse close failed");
     }
 
-    @Test public void preservesBothDeviceCloseFailures() {
+    @Test public void preservesBothDeviceCloseFailures() throws Exception {
         TrackingOutputStream keyboardOutput = new TrackingOutputStream("keyboard", true);
         TrackingOutputStream mouseOutput = new TrackingOutputStream("mouse", true);
         UhidServer server = serverWith(keyboardOutput, mouseOutput);
@@ -150,7 +150,7 @@ public class UhidServerTest {
         assertThat(failure.getSuppressed()[0]).hasMessageThat().isEqualTo("keyboard close failed");
     }
 
-    private UhidServer serverWith(OutputStream keyboardOutput, OutputStream mouseOutput) {
+    private UhidServer serverWith(OutputStream keyboardOutput, OutputStream mouseOutput) throws IOException {
         return new UhidServer(new KeyboardDevice(keyboardOutput), new MouseDevice(mouseOutput));
     }
 
